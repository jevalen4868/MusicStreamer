<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fallenman.apps.musicstreamer" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TopTracksActivity"
            android:label="@string/title_activity_top_tracks"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.fallenman.apps.musicstreamer.MainActivity" />
        </activity>
        <activity
            android:name=".EntityDetailActivity"
            android:label="@string/title_activity_entity_detail"
            android:parentActivityName=".MainActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.fallenman.apps.musicstreamer.MainActivity" />
        </activity>
        <activity
            android:name=".TrackDetailActivity"
            android:label="@string/title_activity_track_detail"
            android:parentActivityName=".TopTracksActivity" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.fallenman.apps.musicstreamer.TopTracksActivity" />
        </activity>
    </application>

</manifest>
